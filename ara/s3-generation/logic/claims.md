# S3 Claims Registry: Generation

Owner: Review Engineer
Writer: Codex
Created: 2026-07-10
Purpose: Track TreeHeap generation-layer claims.

## Claim Status Rules

- `verified`: evidence exists and includes falsification/baseline checks.
- `supported`: positive evidence exists, but baseline/falsification is incomplete.
- `weak positive`: positive evidence exists but strong baselines match or nearly match.
- `open`: plausible but not yet tested.
- `rejected`: tested and failed.

## Semantic Huffman Generation

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-SEM-HUFF-GEN-C01 | S3 should eventually train TreeHeap as a semantic Huffman generation code: encoder, tree code, kernels, and surface decoder jointly minimize reconstruction, masked prediction, route depth, reusable-substructure, and generation losses so that query-useful structures become short, shared, decodable internal subheaps that can generate surface text. | roadmap / blocked by S1 encoder evidence | Design: `semantic_huffman_generation.md`, 2026-07-10. DeepSeek review accepted: this is too large to treat as the next proof. It is retained as a roadmap claim, not an active evidence claim, until the minimal S1 encoder proof shows that TreeHeap can induce useful internal subheaps. | Reject or downgrade if the smaller frozen-encoder decoder gate fails, if BoW/flat baselines match on substructure-controlled generation tasks, if frequency Huffman matches learned semantic Huffman, if echo/readback collapses under compression pressure, if shorter routes do not correspond to reusable query/generation structure, if shuffled corpus preserves the effect, or if training depends on hand semantic labels. |

## Frozen Encoder Decoder Gate

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-FROZEN-DECODER-C01 | Given a frozen TreeHeap encoder that has already induced useful internal subheaps, an S3 decoder should be able to read those internal subheap states and generate a surface label or short surface text better than shuffled/internal-control baselines. | supported numeric / limited gate | `s3_frozen_decoder_gate_probe.py` on io, 2026-07-10, evidence `ara/s3-generation/evidence/s3_frozen_decoder_gate_probe/`. The decoder freezes S1 assignments from `s1_encoder_minimal_observer_probe_026`, builds a `prefix -> P(surface_label)` probability bucket, and performs leave-one-object-out readout. Structured vs shuffled: decoder_top1 `0.4398` vs `0.0903`, decoder_mrr `0.6736` vs `0.3297`, entropy `1.9378` vs `2.2394` bits. | Reject or keep limited if BoW/flat baselines match on same-bag/different-tree generation, if exact sentence generation fails, if the proof depends on gold labels/object IDs as inputs, or if it cannot be reproduced from saved S1 evidence. This gate proves frozen internal bucket readability, not WMT translation or open-ended generation. |

## Same-Bag Different-Tree Generation

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-SAMEBAG-GEN-C01 | If the same leaf sequence and the same bag of tokens can have two different TreeHeap structures with two different surface outputs, then a TreeHeap decoder that reads internal subheaps should generate the correct output better than BoW or flat sequence decoders that do not receive tree structure. | queued proof | Script: `s3_same_bag_tree_generation_probe.py`. Evidence target: `ara/s3-generation/evidence/s3_same_bag_tree_generation_probe/`. Controlled task: `[a,b,c]` with shape `((a b) c)` generates `PAIR a b`, while the same `[a,b,c]` with shape `(a (b c))` generates `PAIR b c`. | Reject if BoW or flat sequence baselines match TreeHeap on OOD exact generation without tree structure, if TreeHeap cannot generalize to held-out triples, if the task leaks shape through token identity/order, or if the result is promoted into WMT/natural-language generation without a real-data follow-up. |

## Task-Loss Structural Emergence

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-TREEHEAP-EMERGENCE-C01 | With only surface generation cross-entropy and no route, depth, merge, category, or compression supervision, a TreeHeap encoder-decoder can learn a non-trivial `stop/left/right` read policy whose held-out generation depends causally on the relevant internal subheap. | queued proof | Script: `s3_treeheap_emergence_probe.py`; design `treeheap_task_loss_emergence.md`; evidence target `evidence/s3_treeheap_emergence_probe/`. The controlled task keeps the same ordered leaves `[a,b,c]` but changes bracketing and the required generated pair. | Reject if TreeHeap does not beat structure-blind BoW/flat sequence controls on OOD triples, if root-only read or zeroing the selected internal subheap does not materially reduce its OOD generation, if its learned route does not prefer the actual internal child, or if the effect survives address/subheap destruction equally well. This does not claim natural-language emergence or a universal loss threshold. |

## Real WMT Seq2Seq Generation

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-WMT-SEQ2SEQ-C01 | A recursively composed TreeHeap encoder can be trained end-to-end from real WMT English text to generate Chinese SentencePiece sequences with only teacher-forced translation cross-entropy; its held-out generation, internal-node ablations, and comparison against BoW/flat-sequence encoders are measurable in one run. | queued feasibility proof | Script: `s3_wmt_treeheap_seq2seq.py`; design `wmt_treeheap_seq2seq.md`; evidence target `evidence/s3_wmt_treeheap_seq2seq/`. The encoder is trained from scratch on WMT17, not initialized from historical collapsed checkpoints. | Reject this feasibility claim if real WMT data cannot be loaded/tokenized, training does not lower held-out NLL versus initialization, greedy decoding is empty/degenerate, or the TreeHeap encoder cannot produce a reproducible comparison with BoW and flat-sequence controls. A lower score than the flat baseline is a valid result, not a reason to hide the run. |

## P0 World-Observation Pretraining

| ID | Claim | Status | Evidence | Falsification |
|---|---|---|---|---|
| S3-P0-WORLD-OBS-C01 | A TreeHeap encoder-decoder can be pretrained from scratch on real unlabeled Chinese text by predicting a held-out continuation span from a preceding TreeHeap context; gradients update shared global parameters, while each document creates only transient TreeHeap state. | running smoke | Script: `s3_treeheap_p0_pretrain.py`; design `p0_world_observation_pretrain.md`; evidence target `evidence/s3_p0_world_observation_smoke/`. P0 reads news, Wikipedia, and web text only, excluding QA/instruction/translation data. | Reject or keep open if the stream cannot be reproduced from manifest data, held-out continuation NLL does not decrease, generated continuations are degenerate, or a TreeHeap full/leaf/root audit gives no evidence that the recursive states are used. This is not a claim of complete world knowledge or QA ability. |

## Residual TreeHeap Forest Pretraining

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-RESIDUAL-FOREST-C01 | A preallocated multi-head parameter TreeHeap forest can be trained on real text through a root-only next-token objective, and the native residual update `H_next = H plus DeltaH` should preserve useful state and gradients better than a same-parameter no-residual TreeHeap while measurably using left/right addresses and more than one kernel head. | rejected as written / full corpus | `evidence/s3_residual_treeheap_forest/full/`, io, 2026-07-14. Complete pass: 38,251,247 blocks from news/wiki/web. Four-head residual first logged NaN at step 62,400 and ended non-finite, while the exactly parameter-matched no-residual model finished at valid NLL `6.2365`. Single-head residual remained finite at NLL `6.4350` and learned residual scale `0.004142`. The evidence rejects the unconstrained global residual-scale implementation, not all possible TreeHeap residuals. | Reopen only with bounded residual gain, FP32 residual accumulation, per-head normalization, non-finite guards, retained pre-divergence checkpoints, and a preregistered requirement to beat the finite no-residual control. |
| S3-TREEHEAP-ROOT-COMPRESS-C01 | A shared multi-head three-slot TreeHeap kernel can recursively compress a 64-token real-text block into one root state from which a decoder predicts the next token, with measurable causal dependence on left/right address pairing and individual kernel heads. | supported full-corpus / single-seed | Same full-corpus evidence. The finite four-head no-residual model reached valid NLL/top1/top5 `6.2365/0.1196/0.2329`; destroying address pairing increased NLL by `2.6252`; ablating heads 0..3 increased NLL by `1.1853/2.4668/1.1574/3.4853`; root variance `0.05843`; gate entropy `1.255` nats vs maximum `ln(4)=1.386`. Decoder receives only the root, with no leaf bypass. | Downgrade if reruns across seeds do not reproduce address/head intervention costs, if matched flat/Transformer/root-pooling baselines match the same compute and bottleneck, if retrained head-removal controls erase the ablation effect, or if less destructive address controls show no effect. This does not prove semantic heads, compression optimality, world knowledge, or architecture superiority. |
| S3-TREEHEAP-PYRAMID-C01 | A frozen TreeHeap root compressor can be extended into a multiresolution codec: root stores coarse task-relevant state and bounded addressed detail codes progressively restore local states, yielding a monotonic rate-distortion curve without changing root next-token NLL. | supported mechanism / three-seed 1M-block proof | `evidence/s3_multiresolution_treeheap_pyramid/main_v2/`, seeds `71401/71402/71403`, one million real-text blocks per seed. MSE for `k=0/8/16/32/64`: `1.9334/0.8803/0.7622/0.6516/0.5526`; k=64 token top-1 `0.9964`, sequence exact `0.8268`; address shift MSE `3.3147`; root audit NLL delta exactly `0`; implemented flat/Haar controls at k=64 `1.6042/1.2881`. All P1-P6 gates passed. | Keep bounded: random-pairing training and per-level ablation were not run, and flat attention had a failure tail. Reject or narrow if independent reruns lose monotonicity/address causality, stronger equal-rate controls dominate, or quantized bit-rate measurements erase the activation-rate gain. Do not claim entropy-coded compression, scale specialization, or universal architecture superiority. |
| S3-TREEHEAP-CODEC-C01 | Starting from random parameters and raw tokens, shared WRITE/FOLD/DETAIL/UNFOLD/READ TreeHeap kernels can learn their own reversible root-plus-addressed-detail protocol using only surface echo cross-entropy, without pretrained embeddings or structural labels. | partial support / continuous detail protocol learned / root-plus rejected | `evidence/s3_treeheap_native_codec/`, io, 2026-07-14. Continuous v1/v2 learned address-causal echo with token top-1 `0.9955/0.9915`, but root-zero remained `0.9956/0.9889`; they are detail-only protocols. A 632-bit STE binary v3 made finite details causal but reached only `0.2073` top-1 and zero sequence exact. All five operators received finite non-zero gradients. | The full root-plus finite-code claim is rejected by the registered P1/P7 gates. Retain only the narrower continuous native-protocol result. Reopen finite compression only with explicit quantized rate-distortion; test root utility with masked/next-span prediction rather than echo. No result here establishes semantics, world knowledge, reasoning, or superiority. |
| S3-TREEHEAP-MASK-C01 | Training on complete aligned subheap-detail removal at multiple depths should move recoverable information from local details toward parent, ancestor, and root states; compared with an exactly matched echo model, deep masked-subheap recovery and root causal contribution should improve as mask depth grows. | partial support / upward signal found / positive-depth trend rejected | `evidence/s3_treeheap_multiscale_mask/smoke/`, io, 2026-07-14. Masked span top-1 for sizes `2/4/8/16/32` was `0.1426/0.0869/0.0750/0.0642/0.0636`. Root-zero added NLL `0.843/0.499/0.330/0.287/0.273`; wrong-root added about `0.34-0.36`, proving sample-specific root use. Five of six gates passed, but root contribution depth Spearman was `-1.0`, exactly opposing P3. | Keep partial: the result supports task-induced upward signal but rejects increasing root reliance with depth. Reopen the stronger claim only after alternating echo/mask batches preserve exact local readback, deep recovery beats unigram/BoW/flat-root controls, and root contribution no longer decays with mask scale. This is not unseen-text inference or world knowledge. |
| S3-TREEHEAP-GEOMETRY-C01 | TreeHeap depth should form scale-indexed geometry rather than increasingly erroneous copies of token space: each node state should preserve the Bag and directed-adjacency observables of its own receptive field, while same-depth state-neighbor Query retrieves spans that are also close in those exact corpus geometries. | not supported / Bag-only positive sub-observation | `s3_treeheap_multiscale_geometry.py`; `evidence/s3_treeheap_multiscale_geometry_smoke/`. P1/P5/P6 passed; P2/P3/P4 failed. Bag read cosine rose from `.267` at 2 tokens to `.436` at 64 and Bag Query gain was positive at every depth. Directed-adjacency read cosine remained `.010-.027`; root shuffle separation was only `.0096`. | Reopen the full claim only with a learnable low-rank non-commutative order observable, matched flat-recursive baseline, and preregistered multi-seed gains. Current evidence supports coarse content contours, not directional topology, human semantics, or world knowledge. |
| S3-TREEHEAP-DIFF-ATLAS-C01 | Several TreeHeap-native perturbations should produce repeatable, operator-specific address-depth response signatures rather than one undifferentiated state difference. | partial support / cross-address operator families / 32K stable | `treeheap_structural_differential_atlas.md`; `s3_treeheap_structural_differential_atlas.py`; `evidence/s3_treeheap_structural_differential_atlas/`. Six-way address-transfer accuracy was `.4776` vs `.1667` chance and stable from 512 to 32K blocks; MIRROR vs child swap was `.7769`. One-vs-two WRITE failed at `.6070`, and Bag/order readout separation failed. | Replicate across checkpoint seeds and training-data scales. Reject a semantic interpretation unless relation-matched minimal pairs and decoder interventions succeed; current evidence establishes stable operator response signatures only. |
| S3-WORLD-ANALOGY-C01 | In a finite compositional world, a parameter World TreeHeap plus `DIFF -> TRANSPORT -> APPLY` should infer `A:B`, transport it to held-out `C`, generate `D`, and causally depend on addressed World TreeHeap state rather than lexical copying. | full World claim not supported / finite analogy subclaim supported | `world_treeheap_analogy_transport.md`; `s3_world_treeheap_analogy.py`; `evidence/s3_world_treeheap_analogy_smoke/`. TreeHeap reached `1.0` sequence exact on all four relation families vs lexical `.5466`, including number shift and role swap. But zero-W token accuracy remained `.9984`, shuffled-W `.9867`, route entropy collapsed near zero, and a 3.2x smaller flat Transformer also reached exact `1.0`. | Reopen only after removing the direct A/B/C-to-APPLY bypass, forcing finite addressed W retrieval, and beating matched flat-memory controls across seeds. Current evidence supports representability of finite analogy, not a causal World TreeHeap or architecture advantage. |

## Private Protocol Data Dose

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-PRIVATE-PROTOCOL-DATA-DOSE-C03 | Under an equal optimizer-update budget and frozen validation/test split, increasing unique WMT training pairs from 30K to 1M should lower h1 TreeHeap held-out NLL if the SPR-064 result was materially data-limited. | supported pilot / single seed / general data effect | `private_protocol_data_dose.md`; code `../src/s3_private_protocol_data_dose.py`; evidence `../evidence/s3_private_protocol_data_dose_full/`. h1 NLL at 30K/100K/300K/1M was `6.2671/5.1454/4.2558/4.0198`: gain `2.2473`, Spearman `-1.0`, three of three adjacent improvements. Flat and Transformer also improved monotonically to `3.9365/3.9201`. | The data-limitation prediction passed, but architecture superiority did not: h1 remained `0.0833/0.0997` NLL behind Flat/Transformer at 1M and was substantially slower. Single seed, one-pass 1M, and noisy web pairs remain. Replicate the endpoint and separate data quality from quantity before asserting a scaling law. |
| S3-STONE1-PRIVATE-PROTOCOL-C01 | On the frozen 1M SPR-065 platform, a fixed-capacity TreeHeap with a shared learned local direction kernel and no leaf/string bypass should learn an encoder-decoder private protocol from final seq2seq NLL, beat identity/frozen-random structural controls, remain address-causal, and produce a usable non-teacher-forced translation CLI. | not supported under registered recipe / address causality supported | `stone1_private_protocol_translation.md`; code `../src/s3_stone1_private_protocol.py`; CLI `../src/treeheap_cli.py`; evidence `../evidence/s3_stone1_private_protocol/`. Formal learned/identity/random mean NLL was `4.2269/4.0719/4.1210`; BLEU-4 was `9.7875/11.1735/10.7198`. Forcing identity improved learned NLL by `0.0118`, forcing random damaged it only `0.0034`, while address swap damaged NLL by `1.4948`. All engineering gates passed, but only 2/5 quality and 1/6 structure gates passed. | This run rejects the hard straight-through local-direction recipe as STONE-1: it was worse and less stable than both controls. It does not reject TreeHeap generally; left/right address causality is strongly supported. Reopen with a preregistered mechanism that makes the learned coordinate protocol stable, and retain identity/random/address interventions. |
| S3-STONE1-PRIVATE-PROTOCOL-C02 | Under fixed handedness, a bilateral reversible base codec `detail=R-L`, `parent=0.4L+0.6R` plus zero-initialized continuous learned residuals in `P/U` should form a task-useful encoder-decoder private protocol, beat fixed algebraic/frozen codec controls, remain address- and depth-causal, and complete the unchanged STONE-1 product gates. | not supported / structural mechanism positive / STONE-1 incomplete | `stone1_canonical_codec.md`; code `../src/s3_stone1_canonical_codec.py`; CLI `../src/treeheap_canonical_cli.py`; evidence `../evidence/s3_stone1_canonical_codec/`. Learned/algebraic/frozen mean NLL was `4.0538/4.1138/4.0910`; BLEU was `11.2865/10.7937/10.6865`. Learned NLL std was `0.0914`. Force-algebraic and address-swap damage was `+1.0984/+1.3545`; all five depth additions improved NLL and root-to-full gain was `0.6491`. | Product NLL/BLEU/stability and frozen-control gates failed, so C02 and STONE-1 remain incomplete. Retain the positive codec/address/depth causal evidence. Do not claim a stable or product-complete private protocol. |
| S3-STONE1-CAPACITY-RATE-DISTORTION-C03 | On the frozen C02 platform, increasing balanced TreeHeap system capacity from 27.62M to 50.27M parameters should reduce held-out distortion reproducibly while retaining codec, address, and depth causality; a 28M double-update control must distinguish capacity from merely training longer. | rejected under registered scaling contract / 28M-long engineering positive / encoder-path causality unresolved / STONE-1 incomplete | `stone1_capacity_rate_distortion.md`; code `../src/s3_stone1_capacity_rate_distortion.py`; evidence `../evidence/s3_stone1_capacity_rate_distortion/`. Mean NLL/BLEU for 28M-long versus 50M-equal was `3.7495/12.7444` versus `4.1469/10.1225`; 50M was also worse than frozen C02 by `0.0930` NLL. The 50M decoder stopped at root: post-fold child-address damage and root-to-full gain were both `0.0`, while force-algebraic damage remained `+1.1021`. | Capacity scaling failed C1/C2/C4/C6/C7 and does not authorize 92M. The post-fold address swap leaves root unchanged, so it proves only that the decoder ignored unfolded children; it cannot falsify encoder path information inside root. Reopen with a pre-fold subtree mirror intervention before labeling root-only reading a structural shortcut. |
| S3-STONE1-PROTOCOL-GROWTH-C04 | With architecture, token loss, data, and optimizer fixed, continued 50M training should reveal whether root-only decoding is an undertrained phase that grows into recursive detail reads or a stable root-compression solution. | supported path-sensitive root compression / recursive decoder growth rejected / single seed | `stone1_protocol_growth_trajectory.md`; code `../src/s3_stone1_protocol_growth_trajectory.py`; evidence `../evidence/s3_stone1_protocol_growth_trajectory/`. From 15,625 to 62,500 updates, validation NLL improved `4.1879 -> 3.6613`, but non-root route mass, root-to-full gain, and post-fold address damage stayed approximately zero. Final pre-fold mirror damage was positive at every depth and reached `0.6747`; force-algebraic damage was `1.3456`. Final test NLL/BLEU-4 was `3.5818/12.5107`. | The simple more-steps recursive-growth prediction failed: the decoder still reads root only. Pre-fold causality supports an ordered, path-sensitive recursive encoder compressed into root, not a bag-like root. Single seed, weak generation quality, and absent recursive decoding keep STONE-1 incomplete. |
| S3-STONE1-FROZEN-PRESSURE-C05 | With the C04 encoder and complete `H_state` frozen, the existing recursive decoder should learn to use deeper TreeHeap information when early stopping is disabled and its downward route is kept open. | supported forced recursive decoder channel / single seed / STONE-1 incomplete | `stone1_frozen_encoder_pressure_decoder.md`; code `../src/s3_stone1_frozen_encoder_pressure_decoder.py`; evidence `../evidence/s3_stone1_frozen_encoder_pressure_decoder/`. Leaf-pressure valid NLL improved `4.6301 -> 3.5496`; test NLL/BLEU-4 was `3.4636/13.9564`, versus root control `3.5149/13.5999`. Branch gradients were always nonzero, encoder checksums were unchanged, and maximum frozen-detail shuffle damage was `0.6909`. | All five registered gates passed. The result proves that C04 frozen `H_state` supports a trainable forced recursive read channel and that intermediate details are causal. It does not show spontaneous depth selection, multi-seed stability, or a completed STONE-1 product. |
| S3-STONE1-DECODER-DEPTH-FLOOR-C06 | A frozen C04 encoder plus a two-percent route-mass floor at every visible decoder depth should preserve gradient flow while allowing the remaining route probability to learn a useful depth distribution. | supported bounded-pressure learnable depth / single seed / STONE-1 incomplete | `stone1_decoder_depth_floor.md`; code `../src/s3_stone1_decoder_depth_floor.py`; evidence `../evidence/s3_stone1_decoder_depth_floor/`. Test NLL for native/forced-leaf/depth-floor was `3.5156/3.4636/3.4117`; BLEU-4 was `13.4823/13.9564/14.4886`. Final floor route mass was `[0.5446,0.0911,0.0911,0.0911,0.0911,0.0911]`, maximum detail-shuffle damage was `0.1311`, encoder checksums were unchanged, and all six gates passed. | The single-seed result supports a fixed pressure supply, not a naturally self-sustaining route. Reopen with multiple seeds, floor-strength ablation, and end-to-end training. Reject a stronger claim if the floor cannot be reduced or removed without renewed route collapse. |
| S3-STONE1-DECODER-RANDOM-PULSE-C07 | Temporary random forced-depth pulses over a frozen C04 encoder should accumulate reusable competence in shared decoder parameters rather than merely overwrite the previously active depth. | smoke completed / formal deferred / small-data accumulation not supported | `stone1_decoder_random_pulse.md`; code `../src/s3_stone1_decoder_random_pulse.py`; smoke evidence `../evidence/s3_stone1_decoder_random_pulse_smoke/` and instrumented repeat. All depth-pair shared-cell gradient cosines were positive (mean about `0.70-0.75`), but all four 4K-data schedules overfit: primary `random_32` mean probe gain was `-0.2858`, maximum forgetting `0.4736`. Depths 2-5 always had nonzero branch gradients; depth 1 is partly topology-degenerate under 64 leaves and source length at most 32 plus EOS. | The smoke rejects a small-data accumulation claim but does not decide the registered one-million-pair formal claim. Formal C07 was deferred when the fixed-root tail convention became the next mechanism question. Reopen only with fixed-coordinate semantics stated explicitly and without treating structurally single-choice branches as gradient failures. |
| S3-STONE1-FIXED-ROOT-NOISE-REPAIR-C08 | Under one fixed 64-leaf root and frozen C04 encoder, the C06 decoder should learn to suppress a visible repeated-EOS tail more effectively than an equally sized deterministic random-token tail, while masked random tail values remain exactly irrelevant. | partial: regular-tail protocol supported / clean-compatible repair rejected / single seed | `stone1_fixed_root_noise_repair.md`; code `../src/s3_stone1_fixed_root_noise_repair.py`; evidence `../evidence/s3_stone1_fixed_root_noise_repair/`, io, one million pairs. Initial clean/masked/EOS/random valid NLL was `3.8472/3.8472/5.8130/5.3125`; matched final clean/EOS/random was `3.5002/3.5370/3.7162`. EOS gained `2.2760` NLL and beat random by `0.1792`, but EOS-trained clean NLL was `3.8258`, `+0.3256` behind the clean-trained decoder. G1/G2/G3/G5/G6 passed; G4 failed. | The run supports repeated EOS as a learnable fixed-frame convention, not a universal lower-layer repair. Clean-compatible repair was falsified by the `0.3256` retention loss above the `0.15` gate. Reopen with an explicit dual-condition or invariant objective; do not relabel protocol specialization as noise removal. |
| S3-STONE1-FROZEN-PLATFORM-REPLICATION-C09 | The released fixed-root EOS TreeHeap recipe should satisfy the STONE-1 product and structural gates across seeds `71901/71902/71903` when every data, model, optimization, and hardware variable is frozen by hash or value. | supported / STONE-1 complete on frozen platform | `stone1_c09_replication.md`; machine contract `stone1_c09_platform_contract.json`; code `../src/s3_stone1_c09_replication.py`; evidence `../evidence/s3_stone1_c09_replication/`. On exactly 1M pairs and 15,625 updates per seed, mean test NLL/BLEU-4 was `3.4524/13.7241`, NLL std was `0.00157`, nonempty was `1.0`, maximum repetition was `0.0215`, and minimum detail-shuffle damage was `+0.5634`. Every Q/S/E gate passed. | Support is bounded to `S3-STONE1-C09-PLATFORM-V1`. Separate data scaling, added epochs, larger models, removable pressure, state-of-the-art translation, and general conversation into later claims. Reopen if independent exact-platform replication breaks the registered quality, structural, or integrity gates. |
| S3-STONE2-TEACHER-UNCERTAINTY-D01 | Under the same TreeHeap student and gold anchor, a frozen translation teacher's top-k probability ordering should improve held-out translation beyond its top-1 string and beyond shuffled top-k weights. | preregistered / smoke first | `stone2_teacher_uncertainty_distillation.md`; code target `../src/s3_stone2_teacher_uncertainty.py`; evidence targets `../evidence/s3_stone2_teacher_uncertainty_smoke/` and `../evidence/s3_stone2_teacher_uncertainty_300k/`. | Reject the uncertainty contribution if top-k does not beat both top-1 and shuffled weights by the registered margins. Do not call teacher probabilities real-world probabilities or hidden-state transfer. |

## Conditional Denoising Seq2Seq

| ID | Claim | Status | Evidence | Falsification |
|---|---|---|---|---|
| S3-DENOISE-SEQ2SEQ-C01 | Raw Chinese text can provide a strongly conditioned seq2seq objective by reconstructing a clean block or missing short span from a damaged block; this should reproduce the stable short generation seen on WMT. | rejected at 1K-step smoke / diagnostic retained | `s3_conditional_denoising_seq2seq.py`; `evidence/s3_conditional_denoising_smoke/`; `evidence/s3_conditional_gap_denoising_smoke/`. Full reconstruction: TreeHeap/Flat/BoW NLL `6.6111/5.7053/6.5707`. Short gap: `7.2434/7.1996/7.2371`. Exact was zero; TreeHeap full and leaf-only were tied in both runs. | Reopen only if a controlled scale-up produces readable held-out recovery and a causal internal-node gain over leaf-only plus matched flat/BoW controls. Current evidence does not support learned topology, persistent memory, world knowledge, or consciousness. |

## Lifting Subheap Pretraining

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-LIFT-SUBHEAP-PRETRAIN-C01 | Under the same real-text missing-span generation loss and optimizer updates, training the reversible learned-update lifting TreeHeap on complete address-aligned subheap masks of widths 1/2/4/8 should improve held-out width-4/8 generation over a target-length-matched arbitrary-span curriculum, while generation remains causally dependent on source, root, multiple depths, and left/right addresses. | partial support / generation and aligned curriculum positive / root-address protocol rejected | Design/result: `lifting_subheap_pretraining.md`; code `s3_lifting_subheap_pretrain.py`; evidence `evidence/s3_lifting_subheap_pretrain_5k/`, io, 5K updates per curriculum. Aligned subheap beat matched random span by `0.0364/0.0540` NLL at width 4/8; source shuffle cost `0.1345`; every detail depth was causal; width-8 generation was non-empty with `0.8262` unique-output fraction. But root zero cost only `0.0042`, adjacent left/right swap only `0.0022`, and exact recovery was zero. P1/P2/P3/P4/P7 passed; P5/P6 failed. | The full root/address claim is rejected at this scale. Retain only the narrower source/detail-dependent seq2seq and small aligned-curriculum result. Reopen a complete TreeHeap protocol only when root and pre-FOLD left/right interventions are causal across seeds, then run a separate WMT transfer comparison against identical from-scratch initialization. |

## Annealed Contraction

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-ANNEAL-TOY-C01 | With complete input and future-only loss, a capacity-constrained TreeHeap contraction should preserve a data-defined predictive event core in root more strongly than independent nuisance details, without core labels entering training. | partial support / predictive-core selection supported / TreeHeap advantage unsupported | io, 3 seeds, 24K train + 4K held-out, 3K updates; `evidence/s3_annealed_contraction_toy/`. T1-T3 passed in 2/3 seeds; core shuffle cost >16 NLL while nuisance shuffle cost approximately zero; probe margin >0.94. T4 failed in every seed: TreeHeap exact gain over the best mean/flat control was `-0.0040`, `0.01525`, and `0.04525`. | Retain only the generic contraction result. Do not claim a TreeHeap-specific benefit until it beats matched non-tree bottlenecks by the registered margin across seeds and held-out compositions. |
| S3-ANNEAL-REAL-C01 | On complete real-text history with no MASK, a leaf-to-root annealed frontier curriculum should train the reversible lifting root to retain sample-specific information useful for future-span generation, producing a measurable rate-distortion curve and causal root/address interventions. | partial support / annealing and rate-distortion positive / sample-address causality rejected | io, 20K updates each for leaf-only, uniform, and annealed; `evidence/s3_annealed_frontier_pretrain/`. Annealed root beat uniform by `0.0918` NLL, root-leaf gap was `0.0603`, frontier Spearman was `-0.75`, and closure/generation gates passed. Source shuffle cost only `0.0852`; sibling swap cost `-0.0011`. R1/R2/R3/R6/R7/R8 passed; R4/R5 failed. | The complete root/address protocol is rejected at this scale. Reopen only with a content- and address-sensitive compose kernel that preserves the annealed gain and makes source shuffle and pre-FOLD left/right swaps causally expensive across seeds. |
| S3-MULTIRES-REPAIR-C01 | After complete input is encoded, a shared TreeHeap repair kernel should use surviving parent/root/sibling/path state to recover erased addressed residuals, restore leaf state and frozen future-generation ability, and degrade gracefully as internal damage grows. | partial support / parent-detail repair supported / address-conditioned repair rejected | io, frozen annealed checkpoint, 8K repair updates, all 6 detail depths x 3 severities; `multiresolution_damage_repair.md`; `evidence/s3_multiresolution_damage_repair/`. Damage cut token retrieval from `1.0000` to `0.5573`. Parent-only restored `0.9095`, recovered `72.38%` latent MSE, and moved future NLL from `15.5926` to `6.2226` versus clean `5.6895`. Cross-scale restored `0.8896` and `69.94%`; wrong-address MSE ratio was `0.968`. P1/P2/P5/P6/P7 passed; P3/P4 failed. | Retain post-FOLD parent-detail repair based on lifting redundancy. Reject root/sibling/path contribution and TreeHeap superiority. Reopen structural recursive repair only after jointly erasing detail and direct-parent information makes grandparent/sibling/address context causally useful across seeds and against flat latent-repair controls. |
| S3-FULL-REPAIR-SEQ2SEQ-C01 | Full local Chinese continuation, QA, instruction, and translation corpora can train the annealed TreeHeap plus parent-detail repair into a source-conditioned, repair-tolerant seq2seq checkpoint with finite long-run optimization and nondegenerate free generation. | running / 60K conditioning and repair positive / generation weak | Design and interim audit: `full_corpus_repair_seq2seq.md`; evidence target `evidence/s3_full_repair_seq2seq/`. At 60K: clean NLL `5.0249`, source-shuffle damage `+0.7924`, damaged/repaired NLL `5.3098/5.0297`, repair fraction `0.983`, and 143.5M valid target tokens. A quadratic continuation-buffer defect caused progressive throughput collapse; the step-60K checkpoint was retained, the input path was repaired at commit `4228df1`, and state-exact training resumed. Free samples remain repetitive and often unrelated. | Keep open until 300K and post-hoc audits. Reject the full claim on unstable completion, insufficient source coverage/tokens, less than 0.20 warm-baseline NLL gain, repair gap above 0.50 or below 50% recovery, phrase-level degeneration, source-shuffle damage below 0.20, or failed exact reload. The adjacent-token repetition gate alone is no longer sufficient. |

## WMT Translation-Loss Learned Fold

| ID | Claim | Status | Evidence | Falsification |
|---|---|---|---|---|
| S3-WMT-LEARNED-FOLD-C01 | Real aligned translation loss can train a hard-forward, soft-backward adjacent-fold kernel so that source TreeHeap topology becomes data-selected rather than fixed by array indices, while retaining measurable Chinese sequence generation. | rejected at smoke / route non-causal | `s3_wmt_learned_fold_seq2seq.py`; `evidence/s3_wmt_learned_fold_smoke/`. Learned/fixed/flat/BoW NLL: `6.0448/6.0205/5.8792/5.8761`. Learned full and leaf-only were tied (`6.0448/6.0434`). Route audit: 490 unique routes in 500 samples and 100% changed after token shuffle, but the decoder did not use internal nodes. | Reopen only after removing the leaf-attention bypass and showing a full-vs-leaf causal gain, superiority to fixed/random routes, and route utility beyond input-dependent diversity. |

## WMT Fixed-Bandwidth Frontier

| ID | Claim | Status | Evidence | Falsification |
|---|---|---|---|---|
| S3-WMT-FRONTIER-C01 | Under the same decoder and fixed `K=4` source-memory budget, a translation-loss learned TreeHeap frontier should beat fixed-tree, random-tree, and flat four-vector frontiers; perturbing learned subheaps should measurably damage held-out translation. | main claim not supported / weak causal route signal | `s3_wmt_frontier_bottleneck.py`; `s3_wmt_frontier_intervention.py`; evidence `evidence/s3_wmt_frontier_smoke/`. Learned/fixed/random/flat NLL `6.5988/6.6012/6.7020/6.5080`. Same-checkpoint fixed/random route replacement increased NLL by `0.0097/0.0285`, below the `0.05` gate. | Reopen only after compose-state information preservation is redesigned and learned frontier beats flat `K=4` plus fixed/random controls across preregistered seeds. Current evidence does not establish a TreeHeap WMT advantage. |

## WMT TreeHeap Functional Equivalence

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S2-TREEHEAP-FUNCTIONAL-EQUIV-C01 | A translation-trained learned TreeHeap frontier should contain repeatable groups of functionally equivalent subheaps: on held-out WMT examples, same-group TreeHeap exchange should damage the target distribution less than a cosine-distance-matched different-group exchange, with stronger separation than fixed/random TreeHeap controls. | not supported at current checkpoint | `wmt_treeheap_functional_equivalence.md`; `s2_treeheap_functional_equivalence.py`; `evidence/s2_treeheap_functional_equivalence/`. Distance matching passed with error `0.000052` and coverage `500/500`. Learned same/different NLL deltas were `0.01865/0.02053`; their gap was only `0.00187`, bootstrap 95% `[-0.00395, 0.00745]`. Fixed/random gaps were `-0.00285/-0.00571`. Functional equivalence remains a relation among TreeHeap states, not a new object type. | The registered `>0.02` learned gap and positive bootstrap interval both failed, as did superiority over TreeHeap controls. Reopen only after a training objective directly rewards reusable subheap behavior or a decoder reads recursively composed states more causally; do not reinterpret geometric clustering as semantic evidence. |

## S2 Emergent-Mask Translation

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S2-TREEHEAP-EMERGENT-MASK-C01 | Translation-only learning with complete multi-scale subheap-descendant masking should push usable information into upper TreeHeap states and improve clean or detail-cut WMT translation over identical plain and equal-count random-mask TreeHeaps. | main claim not supported / weak robustness trend | `s2_treeheap_emergent_mask_translation.md`; `s2_treeheap_emergent_mask_translation.py`; `evidence/s2_treeheap_emergent_mask_translation/`, io FP32, 5K/500/500 WMT, five epochs. Plain/structured/random clean NLL `6.5251/6.5062/6.5033`; cut NLL `6.6989/6.6047/6.6201`. Structured gains were clean `0.01896`, cut-over-plain `0.09419`, cut-over-random `0.01541`; selected-root contribution gain was `-0.00073`. Only finite/non-empty gate passed. | Registered clean, cut, selected-root, and random-control gates all failed. Retain only the narrow observation that memory masking regularized clean/cut translation slightly. Reopen an upward-information claim only when the decoder is forced through recursively selected roots and structured masking beats generic dropout across seeds. |

## S2 Root-Exclusive Decoder

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S2-TREEHEAP-ROOT-EXCLUSIVE-C01 | A variable-resolution root-exclusive TreeHeap frontier should train recursively composed span roots into useful S2 states, improve over full overlapping TreeHeap memory and equal-frontier flat span means, and become causal under compressed-root removal. | partial mechanism support / S2 gain rejected | `s2_treeheap_root_exclusive_decoder.md`; `s2_treeheap_root_exclusive_decoder.py`; `evidence/s2_treeheap_root_exclusive_decoder/`, io FP32, 5K/500/500 WMT, five epochs. Full/exclusive-tree/exclusive-flat native NLL `6.5038/6.6072/6.5770`. Exclusive root removal added `0.10032` NLL versus `0.01877` when the same frontier was imposed on full-tree training; causal gain `0.08155`. Frontier used `0.5126` states/token, with `42.79%` internal roots. | P3/P4/P5 passed, proving the decoder bypass was closed and compressed roots became causal. P1/P2 failed: exclusive TreeHeap lost `0.10345` NLL to full memory and `0.03020` to flat span means. Improve FOLD value preservation before scaling; do not claim S2 quality gain. |

## S2 Lifting-Pump WMT

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S2-LIFT-WMT-C01 | The lifting information pump can represent a real English source for Chinese S2 generation; translation loss should learn root-first recursive probabilistic READ over addressed details, use multiple resolutions, and remain causal under source/root/detail/pairing interventions. | supported mechanism / quality superiority rejected | `s2_lifting_pump_wmt.md`; `s2_lifting_pump_wmt.py`; `evidence/s2_lifting_pump_wmt_full/`, io FP32, 27K/2K/2K WMT, ten epochs. All eight registered gates passed. Recursive/root/full/flat test NLL was `5.0903/5.4337/5.1342/4.8103`. Source/root shuffle damage was `+1.4450/+1.7204`; every detail and pair depth was causal; route mass occupied multiple resolutions; closure MSE was `1.73e-14`. | The mechanism claim passed because recursive READ beat root-only, matched full UNFOLD, used several depths, and failed under source/root/detail/pair interventions. Translation superiority is explicitly rejected: flat sequence remained better by `0.2800` NLL and `0.641` token BLEU-4. Compression and compute claims remain open because this implementation materializes all levels. |
| S2-ADAPTIVE-LIFT-WMT-C01 | A strictly reversible lifting TreeHeap with a learned update kernel and alternating left/right prediction orientation should improve S2 translation over the fixed, permanently left-anchored pump while preserving closure and causal multiresolution READ. | partial: learned update supported / alternation rejected / 0.05 scale gate missed | `s2_adaptive_alternating_lifting_wmt.md`; `s2_adaptive_lifting_wmt.py`; 30K attribution in `evidence/s2_adaptive_lifting_wmt_ablation/`; 200K scale in `evidence/s2_adaptive_lifting_wmt_200k/`. At 30K, learned-update/alternate/combined gains over old were `+0.0892/-0.1251/+0.0394` NLL. At 200K, learned update improved NLL `4.6743 -> 4.6335`, improved BLEU `9.609 -> 9.909`, closed `30.8%` of the flat gap, remained causal at every detail depth and five of six pair depths, and closed at MSE `2.35e-14`. | P5 failed: the registered 200K gain was `0.0408`, below `0.05`. Alternation and combined non-antagonism were rejected in attribution. P6-P11 passed, supporting only the narrower learned-update mechanism. Flat sequence still led by `0.0916` NLL and `0.662` BLEU, so translation superiority remains rejected. |

## Variable-Arity Decoder Depth Growth

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-DECODER-DEPTH-GROWTH-C01 | On long real-text contexts, one shared variable-arity FOLD/READ protocol should produce a useful root-to-leaf decoder growth curve across `k=2..8`, outperform equal-parameter random-tree and flat multiresolution controls at compressed frontiers, remain causal under link shuffling, and partially transfer from trained `k={2,4,8}` to unseen arities. | **invalid as a TreeHeap-depth test / completed multiresolution flat-READ pilot**: P1--P4 failed; P5--P6 passed. Learned recursive-pooling NLL 6.2366, random 6.2361, flat 5.9311. | `decoder_depth_growth.md`; `s3_decoder_depth_growth.py`; `evidence/s3_decoder_depth_growth_pilot/summary.json`. | Post-run audit found that READ discarded parent-child edges and consumed complete level arrays. Dose changed array widths rather than recursive route depth; shuffled-link causality was therefore not testable. Retain the numeric result only for this pooling mechanism. TreeHeap recursive depth remains open. |
| S3-TREE-LIFT-RECURSIVE-C01 | A shared reversible lifting kernel plus parent-to-child probabilistic route should preserve an explicit addressed coarse/detail protocol; frozen recursive depth caps should expose information growth rather than a sequence of independent flat arrays. | contract and frozen WMT depth subclaim supported / broader causal audit open | `recursive_lifting_depth_audit.md`; contract evidence `evidence/s3_recursive_lifting_contract_probe/`; WMT depth evidence `evidence/s3_recursive_lifting_depth_checkpoint_audit/`. Contract closure MSE `3.23e-15`; on one frozen 200K WMT checkpoint, root-only/full NLL was `13.8100/4.6335`, five depth openings improved NLL, and maximum route conservation error was `4.17e-7`. | The positive-depth result proves the same decoder uses recursively opened states, not that root is a human-readable outline. Learned address swaps, source/root causality under the cap protocol, flat comparison, and Chinese future-text replication remain open. |
| S3-PRIVATE-PROTOCOL-BATTLE-C01 | Several private recursive TreeHeap lifting heads, trained only by final WMT seq2seq loss under a fixed total protocol width, should form a causal encoder/decoder protocol and outperform one head and a matched flat sequence baseline. | partial: protocol existence and causality supported; multi-head and flat superiority rejected | `private_protocol_battle.md`; evidence `evidence/s3_private_protocol_battle_full/`. Across three seeds, flat/h1/h2/h4 mean NLL was `6.0401/6.1231/6.1341/6.1934`. Source/root shuffle damage was `+1.9532/+2.1113`; every h4 head helped; cross-seed pair damage was `+2.2507` to `+4.2958`. | P1, structural P3, and private-pair P4 passed. P2 h4-over-h1 and P5 h4-over-flat failed. Do not infer semantic heads or competitive superiority; resolve the fixed-width multi-head bottleneck before Stage B. |
| S3-PRIVATE-PROTOCOL-TF-C02 | On the frozen SPR-064 WMT split and approximately 27M parameter budget, compare TreeHeap h1 with a small 2+2-layer Transformer under both the identical old recipe and a conventional warmup/regularized recipe. | registered small-Transformer comparison supported / top-model claim open | `private_protocol_transformer_benchmark.md`; code `../src/s3_private_protocol_transformer_benchmark.py`; evidence `../evidence/s3_private_protocol_transformer_benchmark_full/`. TreeHeap h1 NLL `6.1231` beat same/standard Transformer `6.4423/6.5330`; parameters differed by `1.236%`. | The small-Transformer competitive gate passed by `0.4099` NLL against the registered standard recipe. Flat GRU remained best at `6.0401`; TreeHeap was much slower, the standard recipe underperformed the same recipe, and its validation loss was still falling. Do not infer industry-top or model-class optimality. |
| S2-LIFT-PRETRAIN-TRANSFER-C01 | Address-aligned missing-subheap generation with a recursive depth curriculum should pretrain the same learned-update lifting TreeHeap used by WMT, then improve matched English-to-Chinese fine-tuning over architecture-identical scratch initialization while retaining multiresolution and address causality. | partial / matched transfer positive / pretraining structure and historical progress failed | `lifting_pretrain_wmt_transfer.md`; code `s2_lifting_pretrain_transfer.py`; evidence `evidence/s2_lifting_pretrain_transfer_full/`, io, 50K pretraining plus matched 200K/5K/5K WMT. Pretrained/scratch test NLL `4.7688/4.8014`, token-BLEU4 `8.3257/7.9869`; transfer gains `+0.03254` NLL and `+0.33881` BLEU passed P2. WMT route/depth/root/detail/pair causality and fair stream passed. Pretraining source shuffle cost only `+0.0554`, root zero was approximately zero, sibling swap cost `+0.0078`; best pretraining was step 2,500 and later updates degraded. | P1 and P3 failed. The run supports matched initialization transfer, not a complete pretraining TreeHeap protocol: pretraining root/address causality was absent, and BLEU remained `1.5834` below historical TreeHeap `9.9091` and `2.2458` below historical flat `10.5715`. Do not scale the same hard-cap curriculum unchanged. |

## STONE-2 Productization

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-STONE2-PRODUCT-C01 | The best legal D01 arm can be deterministically retrained, saved, reloaded and exposed through a translation CLI while retaining its registered quality and TreeHeap structural gates. | registered / queued after D01 | `stone2_productization_route.md`; `../src/treeheap_translate_cli.py`; planned `../evidence/s3_stone2_product_checkpoint/`. | Reject reproducibility if NLL drifts by more than 0.02, token BLEU-4 by more than 0.20, reload changes frozen greedy outputs, repetition exceeds 0.10, or TreeHeap depth/detail gates fail. Translation does not satisfy the later dialogue claim. |
| S3-STONE2-DIALOGUE-C01 | The materialized legal D01 TreeHeap can be continued on 100K Chinese instruction/response pairs to produce nonempty, non-repetitive single-turn answers while retaining causal multi-depth TreeHeap state and exact checkpoint reload. | registered / queued after product checkpoint | `stone2_dialogue_seq2seq.md`; `../src/s3_stone2_dialogue.py`; planned `../evidence/s3_stone2_dialogue_100k/`. | Reject the pilot if validation NLL fails to improve by 0.20, severe repetition exceeds 0.10, outputs are empty, detail/depth gates fail, the encoder receives no gradient, or 32 frozen prompts change after reload. |

## TreeHeap Butterfly Long-Range Communication

| ID | Claim | Status | Evidence | Falsification |
|----|-------|--------|----------|---------------|
| S3-TREEHEAP-BUTTERFLY-LONGRANGE-C01 | A fixed-capacity XOR Butterfly extension composed from local two-node kernels can give TreeHeap leaves an all-address receptive field in `log2(N)` stages, preserve an exact orthogonal information/gradient path, and beat adjacent-only and finite root-bottleneck controls on held-out and longer-width address-conditioned retrieval without dense `N x N` attention. | supported mechanism / synthetic address communication only | `treeheap_butterfly_long_range.md`; `../src/s3_treeheap_butterfly_long_range.py`; `../evidence/s3_treeheap_butterfly_long_range/`, io taskd 78, three seeds. All algebra gates passed. Mean Butterfly held-out/max-distance/unseen-width accuracy was `1.000000/0.999982/0.999980`, versus adjacent-only `0.015729/0.015567/0.015650` and root bottleneck `0.050390/0.050583/0.020776`. | The registered mechanism survived all gates in all seeds. Scope remains synthetic: binary query decomposition is explicit prior. Reopen semantic value only through a matched real-text TreeHeap ablation; do not infer language, WMT, private protocol, or Transformer superiority. |
| S3-TREEHEAP-BUTTERFLY-WMT-C02 | A reversible XOR-Butterfly communication layer between token WRITE and adaptive lifting FOLD should improve real WMT translation over parameter-matched identity and repeated-adjacent schedules, with causal use and a non-weaker effect on long sources. | supported / matched WMT mechanism / three seeds | `treeheap_butterfly_wmt_ablation.md`; `../src/s2_treeheap_butterfly_wmt.py`; formal `../evidence/s2_treeheap_butterfly_wmt_formal/`, io taskd 83. Mean identity/adjacent/Butterfly NLL was `4.65196/4.65415/4.56509`; gains were `0.08687/0.08906`. All mechanism and language gates passed in all three seeds; disabling Butterfly cost mean `1.16873` NLL. | Supported only for this adaptive TreeHeap and WMT recipe. It does not establish semantic XOR addresses, Transformer superiority, production translation, or compute savings. Reopen generality with stronger sequence baselines, different lengths/tasks, and measured sparse-kernel runtime. |
| S3-TREEHEAP-BUTTERFLY-BIDIR-C03 | One shared dynamic-width Butterfly TreeHeap should learn Chinese-English translation in both directions from the complete 14.17M-pair WMT corpus, remain causal under communication ablation, and improve long-length and fixed dream probes during a resumable 96-hour run. | smoke passed / full io taskd 89 active | `treeheap_butterfly_bilingual_full_train.md`; `../src/s3_treeheap_butterfly_bilingual_full.py`; smoke `../evidence/s3_treeheap_butterfly_bilingual_smoke_v2/`. Smoke test NLL was `6.6654/5.8854` en2zh/zh2en and identity damage was `+0.11908`; strict reload reproduced metrics exactly. | Stop or downgrade for non-finite training, direction confusion, persistent source-independent output, no directional validation improvement, failed deterministic reload, or loss of Butterfly causal damage. This claim does not assert large-model or production superiority. |
| S3-TREEHEAP-VIEW-DRIFT-C04 | Some non-monotonic changes in fixed TreeHeap Dreams may be caused by joint Butterfly/FOLD/Decoder coordinate drift rather than semantic forgetting; this requires stable grammatical facts plus held-out recovery between checkpoint intermediate states. | preregistered / retrospective Dreams audit first | `treeheap_viewpoint_drift.md`; expanded `../dreams.txt`; planned code `../src/s3_treeheap_viewpoint_drift.py`; evidence target `../evidence/s3_treeheap_viewpoint_drift/`. | Reject the semantic viewpoint interpretation if held-out state alignment gives no gain, opposite role/polarity/time meanings collapse, semantic-invariant scores fall with surface changes, or trace collection alters native logits/tokens. Alignment alone proves only reparameterization, not semantic correctness. |
| S3-TREEHEAP-CANONICAL-VIEW-C05 | Under a fixed continuation-training budget, a moderate probability of sending the unpermuted leaf order directly into the shared FOLD/Decoder may stabilize canonical translation while retaining measurable use of full Butterfly communication. | not supported / registered one-seed screen negative | `treeheap_canonical_view_ratio.md`; `../src/s3_treeheap_canonical_view_ratio.py`; `../evidence/s3_treeheap_canonical_view_ratio/summary.json`. `p=0` had the best native NLL (3.2710); `p=0.2/0.4/0.6` monotonically worsened it to 3.2826/3.2939/3.3090. Mixing did reduce cross-view JS and identity-route NLL while preserving structural/source causal signals, supporting only a narrower view-invariance trade-off observation. | The registered claim was falsified in this setting because the matched `p=0` arm was best and mixing mainly improved cross-view readability rather than native translation. Do not upgrade without a newly preregistered mechanism and independent evidence. |
| S3-TREEHEAP-CANONICAL-DOSE-C06 | With the native Butterfly dose held fixed, an additional limited identity-view replay may reduce cross-view JS without reproducing most of the native-NLL penalty caused when identity examples replace Butterfly examples; the effect must exceed an equal-compute extra-Butterfly replay control. | not supported as registered / strong one-seed view-specific signal | `treeheap_canonical_view_dose.md`; code `../src/s3_treeheap_canonical_view_dose.py`; evidence `../evidence/s3_treeheap_canonical_view_dose/summary.json`. A/S/BB/BI native NLL was `3.272799/3.282513/3.275903/3.278716`; BB/BI JS was `0.242107/0.104578`. JS specificity `0.137528`, equal-compute native cost `0.002812`, structural gates and exact dose matching passed. Native-dose recovery was only `0.390900`. | The registered screen failed because recovery was below 50%. Retain the narrower evidence that identity replay changes the shared view protocol beyond generic extra compute and that additive replay is cheaper than substitution. Do not upgrade without separately registered multi-seed confirmation. |
