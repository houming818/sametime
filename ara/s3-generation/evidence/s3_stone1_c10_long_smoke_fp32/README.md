# C10 FP32 smoke evidence

Task 35 ran on `io` and passed all smoke gates. The 202,732,913-byte
`checkpoint_latest.pt` remains at the path recorded in `summary.json` on io;
it is intentionally not duplicated in Git. The summary, trace and complete
stdout are retained here.
